# SpineAgent — Python dependencies

# Core
anthropic>=0.42.0
psycopg[binary]>=3.2.0
pgvector>=0.3.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# HTTP (MCP servers use this for Tiendanube + Telegram API calls)
httpx>=0.27.0

# MCP SDK (FastMCP is bundled inside mcp>=1.0.0)
mcp>=1.0.0

# Scheduler (Monitor mode)
apscheduler>=3.10.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
